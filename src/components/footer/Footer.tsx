import * as React from "react";

// import FooterViewer           from "./FooterViewer";
// import { useInfoUserSession } from "../../utils/hooks";

const Footer: React.FC = (): React.ReactElement => {

    // const [footerState, setFooterState] = React.useState<React.ReactElement>(<FooterViewer/>);
    // const infoUserSession = useInfoUserSession();

    // React.useEffect(() => {
    //
    //     setFooterState(<FooterViewer/>);
    //
    // }, [infoUserSession.user.organization_user.roles]);

    // return footerState;

    return <></>
}

export default Footer;